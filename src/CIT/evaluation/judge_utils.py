import json
import logging
import re
from concurrent.futures import ThreadPoolExecutor, as_completed
from uuid import uuid4

import numpy as np
from tqdm import tqdm

from CIT.evaluation.utils import get_retrieved_context

JUDGE_PROMPT_old = """
You are a judge that rates answers. You will be given a user question, a context, a reference answer and a generated answer.
Your task is to provide a 'total rating' scoring how well the generated answer answers the user question regarding the context and the true answer.
Give your answer as an integer on a scale of 1 to 3.
Remember that the information in the generated answer must be in the context.

Here is the scale you should use to build your answer:
1: The generated answer is terrible: it is false, or it contains information that is not in the context, like hallucinations
2: The generated answer contains some true information but it does not fully answer the question, knowing the reference answer answers the question
3: The generated answer is helpful: it contains true information and answers the question, it is complete
If the reference answer says that it does not have the information to answer the question, then the generated answer must also mention that it does not have the information to answer the question, otherwise it will be rated as 1.
If both the reference answer and the generated answer say that they do not have the information to answer the question, then you must rate it as 3.


Now here are the question, the context the reference answer and the generated answer.

User Question::: {question}
Context::: <begin of context> {context}</end of context>
Reference answer::: {true_answer}
Generated answer::: {generated_answer}

Provide your feedback as follows, this is very important. The format of your answer must absolutely follow the following template. Do not add anything else after your rating:
Template:
Feedback:::
Evaluation: your short rationale for the rating, as a text
Total rating: the integer between 1 and 3 (x/3)


Do not add anything else than the feedback and the total rating.
Remember that your goal is to provide a rating between 1 and 3 for the generated answer compared to the reference answer, considering the context.
It is not to solve the question or to provide a complete answer.
Do not try to solve the question. You are a judge.
Feedback:::
The rating between 1 and 3.
Total rating: """

JUDGE_PROMPT = """
You are a judge that classify answers. You will be given a user question, a context, a reference answer and a generated answer.
Your task is to provide a judgment of how well the generated answer answers the user question regarding the context and the true answer.You must say if the answer is helpful or not.
Give your answer as an integer which is 0 or 1.
Remember that the information in the generated answer must be in the context.

Here is the scale you should use to build your answer:
0: The generated answer is false, or it contains information that is not in the context, like hallucinations, or it does not answer the question at all.
1: The generated answer contains some true information, answer the user's question or is helpful, knowing the reference answer answers the question
If the reference answer says that it does not have the information to answer the question, then the generated answer must also mention that it does not have the information to answer the question, otherwise it will be rated as 0.
If both the reference answer and the generated answer say that they do not have the information to answer the question, then you must rate it as 1.


Now here are the question, the context the reference answer and the generated answer.

User Question::: {question}
Context::: <begin of context> {context}</end of context>
Reference answer::: {true_answer}
Generated answer::: {generated_answer}

Provide your feedback as follows, this is very important. The format of your answer must absolutely follow the following template. Do not add anything else after your rating:
Template:
Feedback:::
Evaluation: your short rationale for the rating, as a text
Total rating: the integer 0 or 1


Do not add anything else than the feedback and the total rating.
Remember that your goal is to provide a rating that is 0 or 1 for the generated answer compared to the reference answer, considering the context.
It is not to solve the question or to provide a complete answer.
Do not try to solve the question. You are a judge.
Feedback:::
The rating (0 or 1).
Total rating: """






def evaluate_sample(sample, llm_judge):
    """
    Evaluate a single generated answer by comparing it to the ground truth using a judging LLM.

    Args:
        sample (dict): A dictionary containing:
            - "question": The input question.
            - "RAG_answer": The answer generated by the RAG system.
            - "context": The source context used for generation.
            - "answer": The ground-truth reference answer.
        llm_judge: An LLM instance (e.g. via Ollama) used to evaluate the quality of the generated answer.
        few_shots (bool, optional): Whether to use few-shot prompting with examples. Defaults to False.

    Returns:
        sample (dict): The updated sample dictionary with an added rating score:
            - "rating" if few_shots is False.
            - "rating_few_shots" if few_shots is True.
            If the generated answer is invalid ("Error") or parsing fails, the rating is set to NaN.
    """

    logger = logging.getLogger()
    logger.setLevel(logging.CRITICAL)

    question = sample["question"]
    generated_answer = sample["RAG_answer"]
    if "context" in sample:
        context = sample["context"]
    elif "real_retrieved_context" in sample:
        context = sample["real_retrieved_context"]
    elif "retrieved_context" in sample:
        context = sample["retrieved_context"]
    else:
        context = "No context provided"
    if generated_answer == "Error":
        sample["rating"] = np.nan
        return sample
    true_answer = sample["answer"]
    prompt = JUDGE_PROMPT.format(
        question=question,
        true_answer=true_answer,
        generated_answer=generated_answer,
        context=context,

        )

    feedback = llm_judge.invoke(prompt)  # call the judge llm
    try:  # parse rating
        pattern = r"Total rating: ?\(?(\d)(?:/5)?\)?"
        match = re.search(pattern, feedback.content)
        if match:
            rating = int(match.group(1))
        else:
            print("Parsing error: No rating found in the feedback.")
            print(feedback.content)
            id= str(uuid4())
            json.dump(sample, 
                open(f"./src/CIT/evaluation/results/llm_judge_examples/wrong_cases/example_{id}.json", "w"))
            rating = np.nan

    except Exception as e:
        print(e)
        print("Parsing  eroooooooooooooooooooor")

    sample["rating"] = rating
    return sample

def evaluate_generated_answers_parallel(
    generated_answers, llm_judge, num_workers=4
):
    """
    Evaluate a list of generated answers in parallel using a judging LLM and multithreading.

    Args:
        generated_answers (list): A list of sample dictionaries, each containing:
            - "question"
            - "RAG_answer"
            - "context" or "retrieved_context"
            - "answer"
        llm_judge: An LLM instance used to evaluate the quality of the generated answers.
        few_shots (bool, optional): Whether to use few-shot prompting during evaluation. Defaults to False.
        num_workers (int, optional): Number of parallel threads to use. Defaults to 4.

    Returns:
        results (list): A list of evaluated sample dictionaries, each annotated with a rating field
                        ("rating" or "rating_few_shots" depending on the `few_shots` flag).
    """
    results = []

    with ThreadPoolExecutor(max_workers=num_workers) as executor:
        future_to_sample = {
            executor.submit(evaluate_sample, sample, llm_judge): sample
            for sample in generated_answers
        }

        for future in tqdm(
            as_completed(future_to_sample),
            total=len(generated_answers),
            desc="Evaluating",
        ):
            results.append(future.result())

    return results

def put_real_retrieved_context_on_sample(args):
    sample, compression_retriever, threshold, mapping_id_paths = args
    query = sample["question"]
    context, _ = get_retrieved_context(query,
                                       compression_retriever,
                                       threshold=threshold,
                                       mapping_id_paths=mapping_id_paths)
    sample["real_retrieved_context"] = context
    return sample


def put_real_retrieved_context_on_samples_parallel(questions_with_answers,compression_retriever, threshold, mapping_id_paths):

    args_list=[(sample, compression_retriever, threshold, mapping_id_paths) for sample in questions_with_answers]
    with ThreadPoolExecutor() as executor:
        results = list(tqdm(executor.map(put_real_retrieved_context_on_sample, args_list),
                            total=len(questions_with_answers),
                            desc="Retrieving context"))

    return results